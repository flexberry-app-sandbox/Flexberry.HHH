import { Serializer as ПердстЭкспSerializer } from
  '../mixins/regenerated/serializers/i-i-s-h-h-h-пердст-эксп';
import __ApplicationSerializer from './application';

export default __ApplicationSerializer.extend(ПердстЭкспSerializer, {
  /**
  * Field name where object identifier is kept.
  */
  primaryKey: '__PrimaryKey'
});
